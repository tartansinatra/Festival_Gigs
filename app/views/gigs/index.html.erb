<h1>Festival Gigs</h1>


  <table>
    <!-- <tr>
      <th> Artist </th>
      <th> Date </th>
      <th> Venue </th>
    </tr> -->
    <tr> 
    <% @gigs.each do |gig| %>
      <p> <%= Artist.find(gig.artist_id).title %>,
          <%= Venue.find(gig.venue_id).name %>, 
          <%= nice_date(gig.start_date) %> 
          @<%= nice_time(gig.start_date) %>
          <%= link_to("Show", gig_path(gig)) %>
          <%= link_to("Book!", new_gig_booking_path(gig)) %> </p>
    <% end %>
     
    </tr>
  </table> 
